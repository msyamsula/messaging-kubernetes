apiVersion: v1
kind: Service
metadata:
  name: redis-nodeport
  labels:
    app: redis
    type: nodeport
spec:
  type: NodePort
  selector:
    app: redis
    type: pod
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
      nodePort: 30002